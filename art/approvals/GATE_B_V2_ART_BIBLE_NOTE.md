# Gate B v2 Art Bible Note

Gate B v2 does not broaden the Gate A style. It replaces the rejected Gate B v1 procedural/SVG-heavy implementation with native image-generated and image-refined source assets that follow the same A-H reference rules.

Binding implementation change:

- final runtime assets come from `art/generated/**/raw/` and `art/source/**`;
- final browser screenshots and QA evidence are under `art/final-v2/`;
- `art/final/` remains Gate B v1 evidence and is not approved as final art;
- Gate B v2 is approved by explicit human input on 2026-06-27.
